spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3307/car_sharing_db?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
  data:
    redis:
      host: 127.0.0.1
      port: 6379
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: admin
    password: 123456

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

knife4j:
  enable: true

jwt:
  secret: ${JWT_SECRET:car-sharing-app-2026-secret-key-dev}
  expire-hours: ${JWT_EXPIRE_HOURS:2}

app:
  upload:
    dir: ./uploads

logging:
  file:
    name: logs/system-log-dev.log
  level:
    root: INFO
    cn.ff26710.carsharingapp: DEBUG
